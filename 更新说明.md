# v1.0.2 联系人识别修复

实际页面检查发现：消息 data-id 为独立消息编号，不含联系人 JID；旧消息气泡 class 也不存在。新增使用 conversation-info-header-chat-title 的明确聊天标题字段作为回退标识，避免用个人主页按钮或在线状态文字。

- 旧 JID 标识可用时仍优先使用，保留已有设置。
- 新版页面使用聊天标题保存语言，切换聊天后可恢复。
- 页面中检测到重复聊天标题时不共用设置；退回本次聊天临时语言。
- 名称回退不是稳定的联系人号码：改名需要重新选择语言；未加载到页面的同名联系人无法完整检测，建议使用不同备注。
- 真实当前 WhatsApp 页面只读验证：旧规则 0 匹配，新标题规则成功。未直接更新用户已安装扩展，也未发送消息。
- 模拟验证：两个联系人语言保存/恢复、同名拒绝、旧 JID 优先、原设置保存与模型语音检查通过。

安装：覆盖完整扩展目录，重新加载扩展，刷新 WhatsApp。首次为每位联系人重新选择一次语言，应显示“已记住此聊天的语言”。

# v1.0.1 语言切换修复

语言切换立即生效，不再依赖联系人识别或保存成功。识别成功时长期记住；识别失败时仅当前聊天临时生效，刷新或离开后不保证保留。已为翻译请求传递即时语言选择；接收消息翻译为中文不受影响。

通过未识别联系人切换、请求参数、聊天切换清理、存储失败模拟检查；未在真实 WhatsApp 页面验证。

# v0.4.2 更新与安装说明

## v0.4.2 未识别联系人仍可保存设置
- 未识别到联系人时，设置页正常保存 API、模型和默认语音等配置，不再阻塞所有保存。
- 已有联系人独立语言不会被默认设置覆盖。
- 面板打开时已识别联系人，但保存前聊天变化或断连：先保存全局 API/模型；不把联系人语音误存给其他聊天，并明确提示未保存部分。
- 聊天识别优先读取实际消息气泡对应标识，避免引用区域干扰；增加 role=main 和输入框所属聊天容器兼容。
- 已通过未识别/断连保存、聊天标识、发送和工具栏模拟检查。真实 WhatsApp 页面尚未实测，空聊天仍可能无法建立联系人独立配置。

## v0.4.1 聊天栏直接记住目标语言
- 在聊天输入框上方的语言下拉框选择后，立即保存给当前联系人，并显示“已记住此聊天的语言”。无需进入设置或点保存。
- 切换聊天时恢复该联系人的已保存语言；自定义语言也可在聊天栏直接输入并保存。
- 插件设置页中当前聊天的语言仅展示，保存语音等设置不会覆盖聊天栏保存的语言。
- 仍需能够从消息中识别聊天标识；空聊天或无法识别的页面会提示。
- 语法、联系人隔离及工具栏模拟检查通过，尚未在真实 WhatsApp 页面验证。

## v0.4.0 自动模型缓存、联系人设置和 OpenAI 发音
- 删除手动刷新按钮。每次打开设置，对已填写 Key 的两个服务商自动检查；列表不同才覆盖本机缓存，完全相同不重写；失败保留缓存。更换服务商/Key 时也自动检查。
- 当前选中的模型不会自动替换。最近三代仍按名称版本号推断，保留无版本号别名。
- 打开某位联系人的聊天后打开插件设置：目标语言、自定义语言、发送模式、附带原文、语音引擎、语言声音、语速音调保存给此聊天。API Key、翻译服务商和模型全局共用。
- 聊天栏直接切换目标语言会自动保存给该联系人。设置面板的修改仍需点保存。面板会显示保存对象。
- 联系人配置仅保存在当前浏览器本机。识别依赖 WhatsApp 消息的聊天标识；无消息的新聊天或无法识别的页面会提示，不以联系人姓名冒险合并。此时请在有消息的聊天中配置。
- 新增 OpenAI · GPT-4o mini TTS 引擎，使用 gpt-4o-mini-tts、已有 OpenAI API Key，支持按语言选择内置声音、试听、朗读消息和译文。AI 合成语音按 API 用量计费；单次 4096 字符上限，超出会提示。
- 必须覆盖所有文件（包含新增 settings.js），重新加载扩展并刷新 WhatsApp。
- 模拟检查通过：缓存变更/不变/失败、聊天设置隔离、标识提取、语音请求、发送与只翻译、引用和工具栏回归。未在真实 WhatsApp 或付费语音 API 上实测。
- 语音接口参考：https://developers.openai.com/api/docs/guides/text-to-speech

## v0.3.9 动态模型列表
- 打开设置时查询当前服务商；切换服务商、修改 Key 后自动刷新，也可手动刷新。
- 使用服务商官方 /models 接口和所填 API Key，不再在下拉框维护固定模型清单。
- 根据 GPT / DeepSeek v 名称中的数字版本整理最近三个版本系列，保留其型号变体；这是名称推断，不是官方代际保证。
- 无版本号的别名保留；接口未返回的历史代际不会虚构补齐。模型列表并不保证每个型号支持当前翻译接口，OpenAI 仍可手动填写模型 ID。
- 当前模型始终保留，不自动切换；查询失败、无 Key 或空结果不会清空选择。
- 查询超时 15 秒，旧请求结果不会覆盖新结果。模型筛选、失败处理、选择保留及既有逻辑模拟检查通过；未使用用户真实 Key 实测。
- 官方接口参考：https://developers.openai.com/api/reference/resources/models/methods/list 和 https://api-docs.deepseek.com/api/list-models/

## v0.3.8 发送误判与只翻译按钮
- 插件执行输入框编辑命令时，不再把命令产生的受信任输入事件误认为用户手动编辑。
- 不再比较整个聊天顶部节点和状态文字；保留聊天标题变化、输入框失效、用户修改及侧栏切换保护。
- 新增“只翻译”：替换输入框内容后停止，确认后按回车或点击发送。
- “发送译文”明确执行翻译并发送，不受默认预览模式影响；默认回车流程仍尊重设置。
- 模拟验证：内部编辑不误取消、发送一次、只翻译零发送、真实手动编辑仍取消；已有回归测试通过。
- 尚未在真实 WhatsApp 登录页面验证。

## v0.3.7 按语言记忆声音
- 在“发音设置 → 为此语言设置声音”中选择语言，再选择具体语音。
- 每种语言独立保存浏览器和 ElevenLabs 声音，配置多种语言后统一点击“保存设置”。
- 切换聊天栏目标语言后，朗读译文自动使用对应语言的声音；中文译文可独立设置。
- 自定义目标语言也可绑定；名称忽略大小写及首尾空格。未配置的语言沿用原默认声音。
- 试听示例随内置语言切换；自定义语言使用英文示例。
- 已验证语言切换、保存重载、中文独立配置和浏览器朗读选声；未实测真实 WhatsApp 或 ElevenLabs 在线播放。

## v0.3.6 按钮缺失兼容性修复
- 消息气泡不再依赖 #main 容器或固定 div 标签；增加带消息元数据的消息行回退识别。
- 继续排除顶部栏、输入区、侧边栏和对话框，避免把普通界面文字变成按钮。
- 语言工具栏绑定当前可见输入框所在的 footer；切换聊天、输入区域重建后自动恢复。
- 工具栏在弹性布局中独占一行，防止横向压缩。
- 消息按钮插入正文容器内，避免紧跟文本内联节点产生布局兼容问题。
- 已通过消息正文、语音、发送流程以及工具栏创建/去重/切换/重建的模拟检查。
- 尚未在用户真实 WhatsApp 页面验证，不能确认按钮缺失的唯一原因；更新后仍需刷新 WhatsApp。

## v0.3.5 按钮布局优化
- 消息内按钮左对齐，使用统一的 6px 间距、26px 最小高度和浅绿色背景，减少视觉干扰。
- 底部工具栏统一 34px 高度和 8px 间距；语言选择靠左，发送操作靠右。
- 窄窗口支持自动换行，增加键盘焦点样式和深色模式配色。
- 译文区域统一内边距，并明确隐藏状态，避免 display 样式覆盖 hidden 属性。
- 既有逻辑模拟回归检查通过；浏览器渲染检查因本机 Chromium 启动权限受限未完成，未在真实 WhatsApp 实测。

## v0.3.4 多余浮动按钮修复
- 按钮只添加到当前聊天内真正的收发消息气泡，不再把通用 data-id 或 copyable-text 元素当作消息。
- 排除顶部栏、输入区和对话框中的文本。
- 按钮紧跟正文插入，保持在正文容器内。
- 每次扫描清理无归属、脱离原消息或对应内容已经改变的按钮与译文。
- 通过模拟回归检查；未在用户真实 WhatsApp 页面上实测。
- 更新后必须刷新 WhatsApp 页面，停止旧版本脚本并清除其按钮。

## v0.3.3 引用消息修复
- 按整条消息识别当前正文，排除已识别的引用区域；对未识别的引用容器使用消息末尾正文块。
- 翻译、朗读按钮均在点击时读取当前正文。
- 消息编辑或页面复用消息节点后重新绑定按钮，移除旧译文；过期翻译结果不再插入。
- 新增 10 个模拟正文提取用例，覆盖引用、嵌套格式、多行、阿拉伯语、编辑后的消息与普通消息。
- 尚未在登录后的真实 WhatsApp 页面验证；页面结构变化仍可能需要针对性适配。

## v0.3.2 修复内容
- 浏览器和 ElevenLabs 语音分别保存，切换引擎不覆盖另一种选择。
- 语音列表为空、延迟加载或加载失败时保留已保存的语音；性别筛选不清除当前选择。
- 修复事件参数导致语音选择重置；保存和加载失败会显示错误。
- 预览模式生成译文后，再按回车、原生发送按钮或发送译文按钮可确认发送。
- 翻译期间输入变化、点击聊天区域外或原输入框失效时取消操作；发送前再次检查内容。
- 长消息缓存使用全文并限制最多 200 条。
- 阿拉伯语、俄语等非拉丁字母消息也显示翻译按钮。

## 更新方式
1. 将压缩包解压到一个固定目录。
2. 最稳妥的方式是用新文件覆盖原插件目录内的同名文件，然后在 Chrome/Edge 扩展管理页点击该插件的“重新加载”。请先备份原目录。
3. 刷新 WhatsApp Web。
4. 打开插件设置，分别选择需要的浏览器语音和在线语音，点击保存，再关闭并重新打开设置确认。

保留原扩展并在原目录更新，通常可以沿用现有设置。不要先卸载旧扩展，卸载可能清除设置。旧版本已经丢失的语音选择需要重新选一次。

## 验证情况
已通过 JavaScript 语法检查和模拟回归测试：语音选择持久化、引擎分离、延迟加载、性别筛选、多文字系统翻译按钮、预览确认发送、输入变化及输入框失效后取消操作。
尚未在登录后的真实 WhatsApp 页面或付费语音/翻译 API 上实测。
